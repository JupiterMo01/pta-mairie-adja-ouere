[pytest]
# Répertoire contenant les tests
testpaths = tests

# Affichage verbeux par défaut
addopts = -v --tb=short

# Encodage
# (Python 3 gère nativement l'UTF-8 ; utile sur Windows)
log_cli = true
log_cli_level = WARNING
